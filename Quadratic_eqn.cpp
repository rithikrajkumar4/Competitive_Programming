#include <iostream>
#include <cmath>
#include <complex>
using namespace std;

int main()
{
	int a, b, c;
	int d1 = (-1 * b - (sqrt(pow(b, 2) - (4 * a * c)))) / (2 * a);
	int d2 = (-1 * b + (sqrt(pow(b, 2) - (4 * a * c)))) / (2 * a);
	if (imag(d1) != 0 || imag(d2) != 0)
	{
		cout << Imagin
	}
}