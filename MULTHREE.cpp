#include <iostream>
using namespace std;
int suma(long long a[], int i)
{
	long long s = 0;
	for (int i = 0; i < i; i++)
		s += a[i];
	return s;

}
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
#ifndef ONLINE_JUGDE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif
	int t;
	cin >> t;
	while (t--)
	{
		long long k, d0, d1, s = 0;
		cin >> k >> d0 >> d1;

	}
}