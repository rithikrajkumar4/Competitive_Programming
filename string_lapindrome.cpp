#include <bits/stdc++.h>
using namespace std;

int main()
{
	string s;
	cin >> s;
	int a[(s.length() / 2 + 1)] = {0};
	for (int i = 0, j = (s.length() / 2); i < (s.length() / 2); i++, j++)
	{

	}
	return 0;
}